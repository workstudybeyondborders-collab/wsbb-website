<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title }} - Work & Study Beyond Borders</title>
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      color: #333;
      line-height: 1.8;
      background: #f8f9fa;
    }
    .blog-header {
      background: linear-gradient(135deg, #1a365d 0%, #2d4a7c 100%);
      color: white;
      padding: 20px 40px;
      display: flex;
      align-items: center;
      justify-content: space-between;
    }
    .blog-header a {
      color: white;
      text-decoration: none;
      font-weight: 600;
    }
    .blog-header .logo-text {
      font-size: 1.3rem;
      font-weight: 700;
    }
    .blog-header .back-link {
      background: rgba(255,255,255,0.15);
      padding: 8px 20px;
      border-radius: 25px;
      font-size: 0.9rem;
    }
    .blog-header .back-link:hover {
      background: rgba(255,255,255,0.25);
    }
    .hero-banner {
      background: linear-gradient(135deg, #1a365d 0%, #2d4a7c 100%);
      color: white;
      padding: 60px 40px 80px;
      text-align: center;
    }
    .hero-banner h1 {
      font-size: 2.2rem;
      margin-bottom: 20px;
      max-width: 800px;
      margin-left: auto;
      margin-right: auto;
    }
    .post-meta {
      display: flex;
      gap: 25px;
      justify-content: center;
      flex-wrap: wrap;
      font-size: 0.95rem;
      opacity: 0.9;
    }
    .post-meta span {
      display: flex;
      align-items: center;
      gap: 6px;
    }
    .featured-image {
      max-width: 900px;
      margin: -40px auto 0;
      padding: 0 20px;
    }
    .featured-image img {
      width: 100%;
      height: 400px;
      object-fit: cover;
      border-radius: 12px;
      box-shadow: 0 10px 30px rgba(0,0,0,0.15);
    }
    .post-content {
      max-width: 760px;
      margin: 40px auto;
      padding: 0 20px;
      background: white;
      border-radius: 12px;
      padding: 50px 40px;
      box-shadow: 0 2px 15px rgba(0,0,0,0.06);
    }
    .post-content h2 {
      color: #1a365d;
      margin-top: 35px;
      margin-bottom: 15px;
      font-size: 1.5rem;
    }
    .post-content h3 {
      color: #2d4a7c;
      margin-top: 25px;
      margin-bottom: 10px;
    }
    .post-content p {
      margin-bottom: 18px;
      font-size: 1.05rem;
    }
    .post-content ul, .post-content ol {
      margin-bottom: 18px;
      padding-left: 25px;
    }
    .post-content li {
      margin-bottom: 8px;
    }
    .post-content a {
      color: #e85d04;
      text-decoration: underline;
    }
    .post-content blockquote {
      border-left: 4px solid #e85d04;
      padding: 15px 20px;
      margin: 25px 0;
      background: #fff8f0;
      border-radius: 0 8px 8px 0;
      font-style: italic;
    }
    .post-content img {
      max-width: 100%;
      border-radius: 8px;
      margin: 20px 0;
    }
    .post-footer {
      max-width: 760px;
      margin: 0 auto 60px;
      padding: 0 20px;
      text-align: center;
    }
    .post-footer a {
      display: inline-block;
      background: #e85d04;
      color: white;
      padding: 12px 30px;
      border-radius: 25px;
      text-decoration: none;
      font-weight: 600;
    }
    .post-footer a:hover {
      background: #d04f03;
    }
    .site-footer {
      background: #1a365d;
      color: white;
      text-align: center;
      padding: 30px;
      font-size: 0.9rem;
    }
    .site-footer a {
      color: #e85d04;
      text-decoration: none;
    }
    @media (max-width: 768px) {
      .hero-banner h1 { font-size: 1.6rem; }
      .hero-banner { padding: 40px 20px 60px; }
      .post-content { padding: 30px 20px; }
      .blog-header { padding: 15px 20px; }
      .featured-image img { height: 250px; }
    }
  </style>
</head>
<body>
  <div class="blog-header">
    <a href="/" class="logo-text">WORK & STUDY Beyond Borders</a>
    <a href="/" class="back-link">← Back to Home</a>
  </div>

  <div class="hero-banner">
    <h1>{{ title }}</h1>
    <div class="post-meta">
      {% if author %}<span>✍️ {{ author }}</span>{% endif %}
      {% if date %}<span>📅 {{ date }}</span>{% endif %}
    </div>
  </div>

  {% if image %}
  <div class="featured-image">
    <img src="{{ image }}" alt="{{ title }}">
  </div>
  {% endif %}

  <div class="post-content">
    {{ content | safe }}
  </div>

  <div class="post-footer">
    <a href="/">← Back to Home</a>
  </div>

  <div class="site-footer">
    <p>© 2026 Work & Study Beyond Borders. All rights reserved.</p>
    <p><a href="/">Home</a></p>
  </div>
</body>
</html>
